    depends_on:
      - db
  db:
    image: postgres:13.0-alpine
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=myubp
      - POSTGRES_PASSWORD=myubp123
      - POSTGRES_DB=myubp_db

volumes:
  postgres_data:

    # ESTO VA EN EL DOCKERFILE
    # install psycopg2 dependencies
    RUN apk update \
    && apk add postgresql-dev gcc python3-dev musl-dev

    # Agregar estas dependency al requirement.txt :
    # psycopg2-binary==2.9.1
    # cryptography==3.4.7
    # psycopg2>=2.8
    # Pillow==8.2.0

  # DB
  SQL_ENGINE=django.db.backends.postgresql
  SQL_DATABASE=myubp_db
  SQL_USER=myubp
  SQL_PASSWORD=myubp123
  SQL_HOST=db
  SQL_PORT=5432